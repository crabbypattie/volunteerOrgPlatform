How to run the server: (bash)

1. chmod +x resetImport.sh
2. ./resetImport.sh (if no file in dr, do this: dos2unix resetImport.sh and ./resetImport.sh again)
3. sudo service mysql start
4. install passport-google-oauth20
5. install dotenv
6. npm install
7. npm start